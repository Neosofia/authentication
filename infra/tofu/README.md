# OpenTofu — authentication service (staging + prod)

Infrastructure-as-code for the **staging** and **prod** deployments of the
authentication service. Dev stays on the shell scripts under
[`infra/proxmox/`](../proxmox/) — this directory owns the cloud-backed
environments only.

For the full deployment and operations workflow see [OPS-CLOUD.md](../../OPS-CLOUD.md).
